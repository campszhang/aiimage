/**
 * 放大工具占位文件（未启用）
 *
 * 等 P4-3 决策：是走 sharp Lanczos（简单但非 AI）
 * 还是 Imagen 4.0 upscale API（AI 超分但更贵）。
 *
 * 当前先纯测 Nano Banana 的 imageConfig.imageSize 参数，
 * 如果 Flash 实在不生效，再实装 Imagen upscale。
 */
export {};
